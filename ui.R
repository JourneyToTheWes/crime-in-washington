library(shiny)
library(ggplot2)

shinyUI(fluidPage(
  titlePanel(title = "Crime in Washington"),
  
  sidebarLayout(
    sidebarPanel(
      
    )
  ),
  
  mainPanel(h5("Crime in Washington is different in each county.")
    
  )
  
))